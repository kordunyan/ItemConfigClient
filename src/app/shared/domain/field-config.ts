export class FieldConfig {
    constructor(
        public name: string,
        public type: string,
        public owner: string,
        public printable: boolean,
        public multiple: boolean
    ) {

    }
}
