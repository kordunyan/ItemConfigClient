export class FieldConfig {
    constructor(
        public name: string,
        public type: string,
        public owner: string,
        public isPrintable: boolean,
        public multiple: boolean
    ) {

    }
}